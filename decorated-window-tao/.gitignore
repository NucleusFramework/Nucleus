# Cargo build artifacts (ignored locally; CI rebuilds from sources)
src/main/native/target/

# Cargo.lock — kept in VCS for reproducible cdylib builds across machines.
# (Comment out next line and add `Cargo.lock` here if you'd rather not track it.)
